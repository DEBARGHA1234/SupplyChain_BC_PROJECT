[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Single-page app: serve index.html for any unmatched route.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
